(function () {
  'use strict';

  /**
   * Google Analytics 4 — substitua pelo ID real ou defina GA4_MEASUREMENT_ID no servidor.
   * O endpoint /api/site-config pode sobrescrever este valor em produção.
   */
  window.ACURA_ANALYTICS = {
    ga4Id: 'G-ZXE5T1VCGS',
    loaded: false,
  };
})();
